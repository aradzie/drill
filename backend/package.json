{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node src/cli.ts",
    "build": "node build.js",
    "typecheck": "tsc --noEmit",
    "test": "DB_PATH=:memory: node --test 'src/**/*.test.ts'"
  },
  "dependencies": {
    "@fastify/static": "^10.1.4",
    "fastify": "^5.12.5",
    "shared": "*"
  }
}
